export default function MainPage() {
  return (
    <div style={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      minHeight: "100vh",
      fontSize: 48,
      fontWeight: "bold"
    }}>
      main
    </div>
  );
}

